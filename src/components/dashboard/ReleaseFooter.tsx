"use client";

import { GlobalReleaseFooter } from "@/components/layout/GlobalReleaseFooter";

/** Bloque F — pie de release (wrapper dashboard; estilos en layout). */
export function ReleaseFooter() {
  return <GlobalReleaseFooter />;
}
